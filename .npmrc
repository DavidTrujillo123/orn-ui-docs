; orn-ui es nuestro propio paquete: no aplica la espera de 24h que pnpm impone
; por defecto a releases nuevas (minimumReleaseAge), si no cada publicación
; bloquearía el deploy de la documentación durante un día.
minimum-release-age-exclude[]=orn-ui
